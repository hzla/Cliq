<div id="categories-content-container">
	<div id="cat-content">
		<div id="cat-question" class="head-4 dark-gray">
			<%=  category.ask %>
		</div>
		<%= link_to category_path(category.parent), remote: true, class: 'back-link' do  %>
			<div id="cat-act-back" class="dark-gray head-4">
				<span id="back-text"><< Back to categories</span>
			</div>
		<% end %>
		<div id="finished" class="head-4 gray">
			Finished?
		</div>

		<div id="activities">
			<div id="arrow-container">
				<%= image_tag "arrow.png", id: "arrow" %>
				<div id="arrow-text" class="gray head-4">
					Click to Add
				</div>
			<div id="suggestion-container">
				<div id="suggest-text" class="head-4 dark-gray">
					Don't see you something you like? Suggest we add it!
				</div>
				<%= form_for act, remote: true do |f| %>
					<%= f.text_field :name, :class => "act-name-input" %>
					<%= f.hidden_field :category_id, value: category.id %>
					<%= f.hidden_field :suggested_by, value: current_user.id %>
					<%= f.submit "Suggest", id: "submit-act" %>
				<% end %>
			</div>
			</div>
			<% not_liked.each do |act| %>
				
				<%= render partial: 'interests/act', locals: {act: act} %>
			<% end %>
		</div>

		<div id="chosen-acts">
			<% liked.each do |act| %>
				<%= render partial: 'interests/chosen_act', locals: {act: act, interest: Interest.where(activity_id: act.id, user_id: current_user.id).first} %>
			<% end %>
		</div>
	</div>
</div>